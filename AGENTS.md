# Project Instructions

## Project

**controlcenter** — Control plane for a home LocalAI infrastructure: DGX Spark cluster + NAS model store, recipe-based serving, OpenAI-compatible gateway with per-client telemetry, and power management.

- Languages/components: Not detected
- Build: `npm run build`
- Test: `npm test`
- Run: `npm run dev`
- Task convention: Task identifiers use the `CC-<number>` convention.

Treat values marked “Not configured” or “Not detected” as unknown. Inspect project-owned configuration or ask the user instead of inventing an answer.

## Working Agreement

1. Read the current task and the minimum relevant documentation before editing.
2. Confirm ambiguous requirements and high-impact decisions with the user.
3. Keep changes focused, simple, and consistent with existing conventions.
4. Add or update tests for behavior changes.
5. Run the configured validation, test, and build commands when applicable.
6. Update documentation when public behavior, architecture, configuration, or operations change.
7. Never expose credentials or weaken security controls to make a task pass.
8. For agent-guided bootstrap maintenance, read `docs/AGENT_GUIDED.md`; agents may preview but must hand apply to a human terminal.
9. For versions, releases, or tags, read `docs/RELEASING.md` and use the `semantic-versioning` skill.

## Progressive Disclosure

Do not load all project documentation preemptively.

| Tier | Content | Load when |
|---|---|---|
| T0 | This file | Always |
| T1 | `docs/README.md`, `docs/DEVELOPMENT_PROCESS.md`, and the current task file | Starting implementation work |
| T2 | `docs/ARCHITECTURE.md` and `docs/adr/README.md` | The task changes architecture or asks why the system works a certain way |
| T3 | Matching Proposed or Accepted ADRs, or `docs/RELEASING.md` | Their scope, tags, components, decision, or a release is relevant |
| T4 | Rejected, Deprecated, or Superseded ADRs and historical specifications | Auditing history or resolving conflicting guidance |

For architectural work, read `docs/adr/README.md` first. Load only matching ADRs. Do not read every ADR unless performing an explicit architecture audit.

## Architecture Decisions

Create an ADR when a decision is durable, cross-cutting, security-sensitive, expensive to reverse, or changes data, deployment, dependencies, or integration boundaries. Do not create ADRs for routine refactors or local implementation details.

Use `docs/adr/template.md`. Accepted ADRs are historical records: supersede them with a new ADR rather than rewriting the original decision.

## Validation

```bash
.agentic/bin/validate
.agentic/bin/docs-check
```

Run only commands that are configured and relevant. A missing command is an unresolved project fact, not permission to guess one.

*Generated by agentic-bootstrap 0.1.0.*
